/* 
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */


function getStateList() {
/*    var searchValue = document.getElementById("frmInstitution_im_statemaster").value;

    if (searchValue != "") {
        var msg = $.ajax({
            url:"/pico/ajax/getStateList.action?searchValue=" + searchValue,
            async:false
        }).responseText;
        var listText = unescape(msg);
        var TypeArray = new Array();
        var TypeArrayInfo = new Array();
        TypeArray = listText.split(",");
        document.getElementById("frmInstitution_im_statemaster").options.length = 0;
        document.getElementById("frmInstitution_im_statemaster").options.add(new Option("-- Please Select --", ""));

        for (i = 0; i < TypeArray.length; i++) {
            TypeArrayInfo = TypeArray[i].split("|");
            document.getElementById("frmInstitution_im_statemaster").options.add(new Option(TypeArrayInfo[1], TypeArrayInfo[0]));
        }
    }
    else{
        document.getElementById("frmInstitution_im_statemaster").options.length = 0;
        document.getElementById("frmInstitution_im_statemaster").options.add(new Option("-- Please Select --", ""));
    }*/
    return;
}
