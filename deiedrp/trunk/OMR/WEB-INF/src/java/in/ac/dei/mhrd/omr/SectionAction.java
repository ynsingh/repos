/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package in.ac.dei.mhrd.omr;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

/** 
 * MyEclipse Struts
 * Creation date: 09-27-2010
 * 
 * XDoclet definition:
 * @struts.action path="/section" name="sectionForm" input="/sectionjsp.jsp" scope="request" validate="true"
 * @struts.action-forward name="index" path="/index.jsp"
 */
public class SectionAction extends Action {
	/*
	 * Generated Methods
	 */

	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		SectionForm sectionForm = (SectionForm) form;// TODO Auto-generated method stub
		TestSetUpForm testSetUpForm = (TestSetUpForm) form;
		/*
		try {
            Connection con = Connect.prepareConnection();
            ResultSet rs;

            PreparedStatement ps = con.prepareStatement(
                    "select  TestId, Total_section from TestHeader where TestName = ?");
            
            ps.setString(1, TestSetUpForm.getTestName());
           
            
             rs = ps.executeQuery();
             rs.next();
             System.out.println("Test id " + rs.getString(1));
             System.out.println("Total Section : "+ rs.getInt(2));
            String testid = rs.getString(1);
            int totalSection = Integer.parseInt(TestSetUpForm.getTotalSec());
            System.out.println("Total Section by attri: "+ totalSection);
            for(int i =1;i<= totalSection; i++){
            
            ps = con.prepareStatement("insert into testsetup(TestId, section_number, marks_each_question, Neg_marks, No_of_Question) values (?,?,?,?,?)");
            ps.setString(1, rs.getString(1));
            ps.setInt(2, totalSection);
            ps.setInt(3,Integer.parseInt(sectionForm.getMarksOfEachQues()));
            ps.setInt(4,Integer.parseInt(sectionForm.getNegMarks()));
            ps.setInt(5,Integer.parseInt(sectionForm.getNoOfQues()));
            ps.executeUpdate();
            return mapping.getInputForward();
            }
            }
            catch(Exception e){
            	System.out.println("error in try " + e);
            }
		*/
		return mapping.findForward("index");
	}
}