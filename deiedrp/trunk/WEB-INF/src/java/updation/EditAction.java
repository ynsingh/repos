package updation;
/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import dataBaseConnection.MyDataSource;

import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import java.sql.*;


/** 
 * MyEclipse Struts
 * Creation date: 06-17-2009
 * 
 * XDoclet definition:
 * @struts.action path="/go3" name="editform" input="edit.jsp" scope="request" validate="true"
 */
public class EditAction extends Action {
	/*
	 * Generated Methods
	 */

	/** 
	 * Method execute
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		EditForm editform = (EditForm) form;// TODO Auto-generated method stub
		/*
		 * The variable s holds success or updateoperation.
		 * success for viewdesiredproject.jsp page.
		 * updateoperation for UpdateOperation.jsp page. 
		 * */
		String s="";
		Connection con=null;
			
		try{
			con=MyDataSource.getConnection();//This method Established the connection from the database MySql
		/*
		 * update the 'project' table with the desired values.
		 * */
		PreparedStatement ps=con.prepareStatement("update project set Project_Name=?,Start_Date=?,Finish_Date=?,Target_Budget=?,Priority=?,Status=?,View_Permission=?,GChart_Color=?,Description=? where Project_Id=?");
		ps.setString(1,editform.getPname().trim());
		ps.setString(2,editform.getSdate());
		ps.setString(3,editform.getFdate());
		ps.setInt(4,Integer.parseInt(editform.getTbudget()));//converting String into Integer
		ps.setString(5,editform.getPriority());
		ps.setString(6,editform.getStatus());
		ps.setString(7,editform.getViewPermission());
		ps.setString(8,editform.getGcolor());
		ps.setString(9,editform.getDarea().trim());
		ps.setString(10,editform.getPid());
		int n=ps.executeUpdate();
		if(n>0) /*if n is greater than zero it means update operation is successful.*/
		{
			if(this.getServlet().getServletConfig().getServletContext().getAttribute("update").equals("yes"))
				s="updateoperation";
			else
				s="success";
		}
		
		}
		catch(Exception e)
		{
		}
		finally
		{
			MyDataSource.freeConnection(con);
		}
		return mapping.findForward(s);//calling to that jsp page which is assigned in variable s.
		
	}
	
}