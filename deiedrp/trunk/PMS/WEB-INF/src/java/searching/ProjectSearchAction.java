/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package searching;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;
import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

/** 
 * MyEclipse Struts
 * Creation date: 06-17-2009
 * 
 * XDoclet definition:
 * @struts.action path="/go2" name="projectsearchform" input="projectSearch.jsp" scope="request" validate="true"
 */
/**
 * This class is related with projectSearch.jsp page.
 */
public class ProjectSearchAction extends Action {
	/*
	 * Generated Methods
	 */

	/** 
	 * This method is used for setting the 'soption' and 'svalue' as an attributes in the session scope.
	 * @param mapping It holds the action mapping information used to invoke a Struts action.
	 * @param form This holds the object of the bean class named ProjectSearchForm
	 * @param request The HTTP servlet request we are processing
	 * @param response The HTTP servlet response we are creating
	 * @return ActionForward returns the 'success', which is defined in the struts-config.xml file
	 * For forwarding we use the {@link mapping#findForward(String) findForword} method.
	 * @see "success for call the viewdesiredproject.jsp page"
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		ProjectSearchForm projectsearchform = (ProjectSearchForm)form;// TODO Auto-generated method stub
		HttpSession session=request.getSession();
		/*
		 * setting the search value in session scope.
		 */
		session.setAttribute("svalue",projectsearchform.getKeysearch());
		 if(projectsearchform.getSearchOption()==null)
			 projectsearchform.setSearchOption("Project Name");
		 /*
		  * setting the search option in session scope. 
		  */
		 session.setAttribute("soption",projectsearchform.getSearchOption());
		
		/*
		 * success for viewdesiredproject.jsp page that shows data about search element. 
		 * */
		return mapping.findForward("success");
		
	}
}