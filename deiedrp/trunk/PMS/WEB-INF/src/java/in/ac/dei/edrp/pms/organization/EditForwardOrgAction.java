/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package in.ac.dei.edrp.pms.organization;


import in.ac.dei.edrp.pms.dataBaseConnection.MyDataSource;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.sql.rowset.CachedRowSet;


import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

import com.sun.rowset.CachedRowSetImpl;

/** 
 * MyEclipse Struts
 * Creation date: 11-03-2009
 
 * XDoclet definition:
 * @struts.action path="/editorg1" name="orgform" scope="request" validate="true"
 * @struts.action-forward name="success" path="page.editorgpage"
 */
/**
 * This class is responsible for setting the value of Organisation Id 
 * and then returns the mapping forward String for next view
 */
public class EditForwardOrgAction extends Action {
	/*
	 * Generated Methods
	 */

	/** 
	 * Method execute
	 * In attribute "key",It sets the value of Organisatiopn ID 
	 * and then returns the forward String for next view.
	 * @param mapping It holds the action mapping information  used to invoke a struts action
	 * @param form It holds the object of bean class,named OrgForm
	 * @param request The HTTP servlet request,which is going to be processed
	 * @param response The HTTP servlet response,which is going to be created.
	 * @return ActionForward returns editorgpage for next view.
 	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		Connection con=null;
		String forwardmsg="editorgfail";
		
		try{
			/*
			 * This method Established the connection from the database MySql
			 */
		con=MyDataSource.getConnection();
		PreparedStatement ps=con.prepareStatement("select * from organisation o"+
		" where o.Org_Id=?");
	    ps.setString(1,request.getParameter("id"));
	    ResultSet rs_org=ps.executeQuery();
	    CachedRowSet crs = new CachedRowSetImpl();
	    crs.populate(rs_org);
	    
	    if(crs.next())
	    {
	    	request.setAttribute("crs", crs);
	    	forwardmsg="editorgpage";
	    }
		//System.out.println("org id="+request.getParameter("id"));
		}
		catch(Exception e){}
		finally
		{
			MyDataSource.freeConnection(con);
		}

		return mapping.findForward(forwardmsg);
	}
}