/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package organization;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

/** 
 * MyEclipse Struts
 * Creation date: 10-27-2009
 * XDoclet definition:
 * @struts.action path="/go4" name="searchorgform" input="searchorg.jsp" scope="request" validate="true"
 */
/**
* This action class is responsible for setting the value of Search String and Option in attributes.
  */
public class OrgSearch extends Action {
	/*
	 * Generated Methods
	 */

	/** 
	 * Method execute
	 * It sets the attribute which contains the search option value and value of search String
	 * and then returns the forward String for next view
	 * @param mapping It holds the action mapping information  used to invoke a struts action
	 * @param form It holds the object of bean class,named SearchOrgForm
	 * @param request The HTTP servlet request,which is going to be processed
	 * @param response The HTTP servlet response,which is going to be created.
	 * @return ActionForward returns success or fail according to condition
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		SearchOrgForm searchorgform = (SearchOrgForm)form;// TODO Auto-generated method stub
		/**
		 * Set the 'keySearch' as an attribute that holds search element.
		 * */
		HttpSession session=request.getSession();
		 session.setAttribute("keysearch",searchorgform.getKeysearch());
		 if(searchorgform.getSearchOption()==null)
			 searchorgform.setSearchOption("Organisation Name");
		 session.setAttribute("searchOption",searchorgform.getSearchOption());

		 return mapping.findForward("success");
		
	}
}